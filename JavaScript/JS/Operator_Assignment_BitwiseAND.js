// Operators

//Assignment operators (=, +=, -= etc.)
a = 5;
b = 3;

a &= b;
console.log("After AND B to A :\n Value of A : "+a+ "\n Value of B : "+b);
// It does a bitwise AND operation on the operand, and assigns the result to the variable.