// It checks whether two operands are non-zero (0, false, undefined, null, or “” is considered as zero),
// if yes then return the first operand when evaluating from left to right. 
a = 5;
b = 3;



if(a==0 || b==0){
    console.log("a  and  b one of them is non-zero");    
}else{
    console.log("a  and  b both are non-zero");}