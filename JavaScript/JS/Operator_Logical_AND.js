// It checks whether two operands are non-zero (0, false, undefined, null, or “” are considered as zero),
// if yes then return the last operand when evaluating from left to right.

a = 5;
b = 3;


//Comparison operators (==, !=, >, <, >=, <= )

if(a==0 && b==0){
    console.log("a  and  b both are zero");    
}else{
    console.log("a  and  b both are non-zero");}